<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <svg class="footer__logo-icon" viewBox="0 0 32 32" width="24" height="24" aria-hidden="true">
        <g fill="none" stroke="rgba(255,255,255,0.55)" stroke-width="3" stroke-linecap="round">
          <path d="M7 24 A 12 12 0 0 1 13 7"/>
          <path d="M19 6 A 11 11 0 0 1 26 13"/>
          <path d="M27 17 A 11 11 0 0 1 17 26"/>
        </g>
      </svg>
      <span class="footer__brand-name">well at work</span>
    </div>
    <p class="footer__copy">Indsigt med respekt for mennesker · &copy; {new Date().getFullYear()} Well at Work</p>
  </div>
</footer>

<style>
  .footer {
    background-color: var(--navy-deep);
    padding: 2rem 0;
  }

  .footer__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .footer__brand {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .footer__brand-name {
    font-family: var(--font-body);
    font-weight: 500;
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.65);
  }

  .footer__copy {
    font-size: var(--text-small);
    color: rgba(255, 255, 255, 0.4);
  }
</style>
