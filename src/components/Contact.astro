<section class="contact section section--navy" id="kontakt">
  <div class="container">
    <div class="contact__inner">
      <div class="contact__text">
        <h2 class="contact__title">Lad os starte med en samtale</h2>
        <p class="contact__sub">
          Book en uforpligtende demo — vi viser jer, hvad jeres organisation kan lære af 15 minutter.
        </p>
        <div class="contact__info">
          <a href="mailto:info@bewellatwork.dk" class="contact__info-link">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="2" y="4" width="20" height="16" rx="2"/>
              <path d="M22 4 12 13 2 4"/>
            </svg>
            info@bewellatwork.dk
          </a>
          <a href="tel:+4500000000" class="contact__info-link">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2A19.86 19.86 0 0 1 3.08 4.18 2 2 0 0 1 5.08 2h3a2 2 0 0 1 2 1.72c.13.81.36 1.6.68 2.35a2 2 0 0 1-.45 2.11L8.09 10.4a16 16 0 0 0 6.51 6.51l2.22-2.22a2 2 0 0 1 2.11-.45c.75.32 1.54.55 2.35.68A2 2 0 0 1 22 16.92z"/>
            </svg>
            +45 00 00 00 00
          </a>
        </div>
      </div>

      <form class="contact__form" action="#" method="POST">
        <div class="contact__field">
          <label for="contact-name" class="sr-only">Navn</label>
          <input type="text" id="contact-name" name="name" placeholder="Navn" required />
        </div>
        <div class="contact__field">
          <label for="contact-company" class="sr-only">Virksomhed</label>
          <input type="text" id="contact-company" name="company" placeholder="Virksomhed" required />
        </div>
        <div class="contact__field">
          <label for="contact-email" class="sr-only">Email</label>
          <input type="email" id="contact-email" name="email" placeholder="Email" required />
        </div>
        <div class="contact__field">
          <label for="contact-employees" class="sr-only">Antal medarbejdere</label>
          <input type="text" id="contact-employees" name="employees" placeholder="Antal medarbejdere" />
        </div>
        <div class="contact__field">
          <label for="contact-message" class="sr-only">Besked (valgfri)</label>
          <textarea id="contact-message" name="message" rows="4" placeholder="Besked (valgfri)"></textarea>
        </div>
        <button type="submit" class="btn btn--white contact__submit">Send besked</button>
      </form>
    </div>
  </div>
</section>

<style>
  .contact {
    padding-top: clamp(4rem, 8vw, 7rem);
    padding-bottom: clamp(4rem, 8vw, 7rem);
  }

  .contact__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .contact__title {
    font-family: var(--font-heading);
    font-size: var(--text-h1);
    color: var(--white);
    margin-bottom: 1rem;
  }

  .contact__sub {
    font-size: var(--text-body-lg);
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  .contact__info {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .contact__info-link {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    color: rgba(255, 255, 255, 0.75);
    font-size: var(--text-body);
    transition: color 0.2s ease;
  }

  .contact__info-link:hover {
    color: var(--white);
  }

  .contact__form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .contact__form input,
  .contact__form textarea {
    width: 100%;
    padding: 14px 16px;
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: var(--btn-radius);
    background: rgba(255, 255, 255, 0.08);
    color: var(--white);
    font-size: var(--text-body);
    transition: border-color 0.2s ease, background 0.2s ease;
  }

  .contact__form input::placeholder,
  .contact__form textarea::placeholder {
    color: rgba(255, 255, 255, 0.5);
  }

  .contact__form input:focus,
  .contact__form textarea:focus {
    outline: none;
    border-color: rgba(255, 255, 255, 0.5);
    background: rgba(255, 255, 255, 0.12);
  }

  .contact__form textarea {
    resize: vertical;
    min-height: 100px;
  }

  .contact__submit {
    align-self: flex-start;
    margin-top: 0.5rem;
  }

  @media (max-width: 768px) {
    .contact__inner {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
  }
</style>
