<section class="hero" id="hero">
  <div class="container hero__inner">
    <p class="hero__eyebrow">Atlas — fra Well at Work</p>
    <h1 class="hero__headline">
      Et levende kort over jeres organisation — og&nbsp;hjælpen til at ændre det.
    </h1>
    <p class="hero__sub">
      De fleste værktøjer stopper ved målingen. Atlas går videre: hver medarbejder får
      et personligt forløb, hver leder en coach — og I kan måle virkningen.
    </p>
    <div class="hero__ctas">
      <a href="#kontakt" class="btn btn--primary">Book en demo</a>
      <a href="#kortet" class="btn btn--secondary">Se hvordan det virker</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding-top: clamp(4.5rem, 10vw, 8rem);
    padding-bottom: clamp(9rem, 17vw, 15rem);
    background-color: var(--parchment);
    background-image: url('/images/hero-topography.png');
    background-repeat: no-repeat;
    background-position: bottom center;
    background-size: 100% auto;
    overflow: hidden;
  }

  .hero__inner {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 880px;
    margin: 0 auto;
  }

  .hero__eyebrow {
    font-size: 0.78rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--copper);
    margin-bottom: 1.5rem;
  }

  .hero__headline {
    font-family: var(--font-heading);
    font-weight: 600;
    font-size: var(--text-hero);
    color: var(--navy);
    line-height: 1.12;
    letter-spacing: -0.015em;
    margin-bottom: 1.5rem;
  }

  .hero__sub {
    font-size: var(--text-body-lg);
    color: var(--navy);
    max-width: 640px;
    margin: 0 auto 2.5rem;
    line-height: 1.75;
    opacity: 0.85;
  }

  .hero__ctas {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
</style>
